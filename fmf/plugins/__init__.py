"""
FMF Plugins Package

Built-in plugins live in this package and are advertised to the plugin
registry through the ``fmf.plugins`` entry point group (see ``pyproject.toml``).
They are discovered automatically at runtime; there is no hard-coded plugin
list to maintain here.
"""
